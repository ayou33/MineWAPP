export const DEFAULT_PACKAGE_NAME = 'com.mine.app'

export const DEFAULT_MARKET = 'Web'

export const DEFAULT_APP_VERSION = '0.0.0'

export const DEFAULT_PKG = '_dft_pkg'

// ─── Project-specific domain constants ───────────────────────────────────────
// Adjust these to match your backend's conventions.

/** Multiplier between display amounts and stored integer amounts (cents/fen/etc.). */
export const MONEY_MULTIPLIER = 100
